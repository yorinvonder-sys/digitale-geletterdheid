# Production Environment Variables
# Copy this file to .env.production and fill in the values
# NEVER commit .env.production to version control

# Gemini AI API Key (server-side only — never expose in client bundle)
# Used via Supabase Edge Function proxy, NOT directly in frontend
VITE_GEMINI_API_KEY=REPLACE_ME

# Supabase (from Supabase Dashboard > Project Settings > API)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
