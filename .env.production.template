# Production Environment Variables
# Copy this file to .env.production and fill in the values
# NEVER commit .env.production to version control

# Supabase (from Supabase Dashboard > Project Settings > API)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# NOTE: AI (Gemini) auth is handled server-side via Vertex AI service account.
# The service account key is stored as a Supabase secret (GOOGLE_SERVICE_ACCOUNT_KEY).
# No client-side AI credentials needed.
